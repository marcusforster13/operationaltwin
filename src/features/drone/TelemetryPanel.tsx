import type {DroneTelemetry} from '../../types/domain';
export function TelemetryPanel({data,recorded=false}:{data:DroneTelemetry|null;recorded?:boolean}){const value=(n:number|null|undefined,suffix:string)=>n==null?'N/D':`${n.toFixed(1)} ${suffix}`;return <section><h3>DRONE 02 · {recorded?'RECORDED':'SIMULATION'}</h3><div className="telemetry"><div>ALTITUDE LOCAL Y<strong>{value(data?.altitude,'u')}</strong></div><div>VELOCIDADE<strong>{value(data?.speed,'u/s')}</strong></div><div>HEADING LOCAL<strong>{value(data?.heading,'°')}</strong></div><div>BATERIA SIMULADA<strong>{value(data?.battery,'%')}</strong></div></div><p className="muted">{data?.state??'idle'} · {recorded?'conexão gravada: ':''}{data?.connection??'offline'}</p></section>}
