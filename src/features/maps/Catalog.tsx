import {MAPS} from '../../config/maps';
export function Catalog({onSelect}:{onSelect:(id:string)=>void}){return <section className="catalog"><div className="eyebrow">CENTRO DE OPERAÇÕES / V1</div><h1>O território em contexto.</h1><p>Selecione uma localidade para planejar, simular e reconstruir operações.</p><div className="map-grid">{MAPS.map((m,i)=><article key={m.id}><span className="map-index">0{i+1} / {m.id.toUpperCase()}</span><div className="map-art" aria-hidden="true">◇</div><h2>{m.name}</h2><p>{m.city} / {m.state}</p><p className="muted">{m.description}</p><small>Metadata geoespacial detectada<br/>Calibração de campo: PENDENTE</small><button onClick={()=>onSelect(m.id)}>ABRIR GÊMEO DIGITAL →</button></article>)}</div></section>}
