import type {DemoRunner,DemoState} from './DemoRunner';
export function DemoPanel({runner,state}:{runner:DemoRunner;state:DemoState}){return <section className="demo-banner"><div><strong>DEMONSTRAÇÃO — DADOS SIMULADOS</strong><p role="status">{state.paused?'PAUSADA · ':''}{state.message}</p>{state.error&&<p role="alert">{state.error}</p>}{state.stage==='complete'&&<p>{state.frames} frames · {state.duration.toFixed(1)} s gravados · Incidente 047 · 3 recursos · 5 waypoints · Replay concluído</p>}</div><div className="controls"><button onClick={()=>runner.start()}>{state.stage==='idle'?'INICIAR':'REINICIAR DEMO'}</button><button disabled={['idle','complete','error'].includes(state.stage)} onClick={()=>state.paused?runner.resume():runner.pause()}>{state.paused?'RETOMAR DEMO':'PAUSAR DEMO'}</button><button onClick={()=>runner.cancel()}>CANCELAR DEMO</button></div></section>}
